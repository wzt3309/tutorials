spring:
  datasource:
    url: jdbc:h2:mem:dbtest
    username: sa
    password:
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 20
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: H2